import './OneBigOneSmallCol.css';

const OneBigOneSmallCol = (props) => {
    return (
        <div className={'oneBigOneSmallCol'}>
            {props.children}
        </div>
    )
};

export default OneBigOneSmallCol;